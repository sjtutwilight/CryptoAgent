## 约束
- 你的回复以及文档编写始终用中文进行
- 执行 openspec apply/change 实施前，必须先创建并切换到新分支。
分支命名：openspec/<change-name>-<yyyymmdd-hhmm>
若当前不在该分支，先执行：
./tool/git-branch.sh new openspec/<change-name>-<yyyymmdd-hhmm>
然后再开始实现任务。
