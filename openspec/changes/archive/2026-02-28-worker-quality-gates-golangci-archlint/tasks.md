## 1. 质量门禁基础配置

- [x] 1.1 在 `datainjector/worker` 新增 `.golangci.yml`，启用首批规则（含 `staticcheck` 与复杂度/可维护性相关规则）
- [x] 1.2 在 `datainjector/worker` 新增 `go-arch-lint` 配置文件，定义核心包允许依赖关系
- [x] 1.3 固定两类工具版本并在配置/脚本中声明最低可用版本

## 2. 统一执行入口与结果输出

- [x] 2.1 新增统一执行脚本（如 `datainjector/worker/tools/quality_gate.sh`），串行执行两项检查
- [x] 2.2 为统一脚本增加失败即退出与阶段化日志输出
- [x] 2.3 增加机器可读结果输出（JSON 文件或 stdout JSON）以供 Codex/CI 解析

## 3. 基线与阻断策略落地

- [x] 3.1 首次在当前代码基线上执行检查并生成历史问题基线文件
- [x] 3.2 在统一脚本中实现“基线外新增违规”识别逻辑
- [x] 3.3 校验策略：仅命中基线可通过，出现新增违规必须失败

## 4. 流程集成与文档

- [x] 4.1 在 worker README 或开发文档补充安装与执行说明（本地/CI）
- [x] 4.2 在现有 CI 流程中接入统一脚本并开启失败阻断
- [x] 4.3 增加最小回归验证（至少覆盖：成功路径、lint 失败、架构规则失败三类场景）
