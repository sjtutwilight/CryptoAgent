你正在执行 AAVE 数据接入的无人值守故障排查。

目标：
1. 先定位根因（容器状态、roles、Kafka topic 增量、关键错误日志）。
2. 以最小改动修复问题，避免扩大影响。
3. 修复后必须重启必要组件并继续观测一段时间，不要立即结束。
4. 只有在“角色完整 + topic 持续增长 + 高信号错误清零”同时满足时才判定恢复。

执行要求：
- 优先使用现有脚本和接口，不要引入额外复杂组件。
- 若命中 Binance 风控相关错误，说明风险范围并给出保守处理策略。
- 输出必须包含：根因、修复动作、验证证据、当前状态、后续建议。
