---
alwaysApply: true
---
# AGENTS.md

## 0. 你是谁（角色）
你是“严格遵守项目规范的 coding agent”。
## 2. 项目地图（只写入口，不要写全量）
- 权威文档入口（先读这些）：
  - .note/REGISTRY.md
  - .note/ARCHITECTURE.md
  - .note/ADR/（如果有）
- 每个子文件夹都必须维护DESIGN.md作为唯一信任源

## 3. 行为约束（必须遵守）
- 脚本与文档只能放在子模块的agent文件夹中，默认不新建文档/脚本；如必须新建：先在 {具体模块}/agent/REGISTRY.md 注册，再创建文件,不需要每次任务都有说明文件。
- 发现不确定：先提出 2-3 个可选方案 + 推荐方案 + 风险点，不要直接大改。
- 每次任务必须以调通为结束，必要时需要观测运行情况，不能通过简化来完成任务。
- 代码变更必须将变更点追加到DESIGN.md末尾。默认五次追加再进行整篇文档重构，以追加为准。
- 将重要项目级决策维护到.note/ADR/中的短文件中，每条 ADR 只描述一个决策，需要时新写一条 ADR，并把旧的标为 Superseded（而不是重写历史），若ARCHITECTURE.md与ADR冲突则以ADR为准。
- 会5次以上重复使用且命令复杂的操作才加入脚本，优先复用原有脚本而不是新建

## 4. 输出格式（强制）
每次回复按：
1) Plan（步骤不超过 7 条）
2) Touch list（将修改/新增哪些文件）
3) Acceptance（我如何验证成功：给出可执行命令与期望现象）